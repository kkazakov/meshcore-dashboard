services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    image: meshcore-dashboard:latest
    container_name: meshcore-api
    ports:
      - "8080:8080"
    restart: unless-stopped
